import { createSlice, PayloadAction } from '@reduxjs/toolkit';
import { T{{ properCase sliceName }}State } from './types';

export const initialState: T{{ properCase sliceName }}State = {};

const slice = createSlice({
  name: '{{ camelCase sliceName }}',
  initialState,
  reducers: {
    someAction(state, action: PayloadAction<any>) {},
  },
});

export const {
  actions: {{ camelCase sliceName }}Actions,
  name: {{ camelCase sliceName }}SliceKey,
  reducer: {{ camelCase sliceName }}Reducer,
} = slice;
