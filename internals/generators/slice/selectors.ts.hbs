import { createSelector } from '@reduxjs/toolkit';
import { TRootState } from 'store/TRootState';
import { initialState } from '.';

const selectSlice = (state: TRootState) => state.{{ camelCase sliceName }} || initialState;

export const select{{ properCase sliceName }} = createSelector([selectSlice], state => state);
